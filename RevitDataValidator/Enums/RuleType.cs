namespace RevitDataValidator
{
    public enum RuleType
    {
        INVALID,
        List,
        Regex,
        PreventDuplicates,
        FromHostType,
        FromHostInstance,
        Calculated
    }
}